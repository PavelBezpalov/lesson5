var form = $('#add_child_comment');
<% if @parent_comment %>
  form.parent().after($('<%= j render @comment %>'));
<% else %>
  form.parent().parent().prepend($("<%= j render @comment %>"));
<% end %>
form.remove();
$('#<%= dom_id(@comment) %>').hide().fadeIn('slow');